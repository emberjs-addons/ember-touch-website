
<div class="alert alert-info">
  <p>You could block Application gestures recognition with <code>App.gestureManager.isAllBlocked</code> property. Toggle the button and test the views at the right side.</p>
</div>


<div class="row-fluid">

  {{#view App.ApplicationGestureManagerButtonView class="span6" }}

    {{#if view.isGesturesBlocked}}
      Blocking all the gestures
    {{else}}
      Gestures can be recognized
    {{/if}}

  {{/view}}

  {{view App.LetterSwipeView class="span6"}}

</div>

<div class="space"></div>

<div class="row-fluid">

  {{view App.PanPhotoCanvasView class="offset6 span6"}}
</div>


<div class="space is-code"></div>

<div class="row-fluid">

  <div class="span12">
    <span class="label label-info">Code</span>
    <pre>
App.ApplicationGestureManagerButtonView = Em.View.extend({
  isGesturesBlockedBinding: '<b>App.gestureManager.isAllBlocked</b>',

  click: function() {
    set(this, 'isGesturesBlocked', !get(this, 'isGesturesBlocked') );
  },

  willDestroyElement: function() {
    App.gestureManager.set('isAllBlocked', false);
  }

});
    </pre>
  </div>
</div>
